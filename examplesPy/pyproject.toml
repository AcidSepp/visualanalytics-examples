[project]
name = "examples"
version = "0.1"
dependencies = [
    "requests",
    "numpy",
    "pendulum",
    "scipy",
    "matplotlib",
    "pandas"
]
requires-python = ">3.14"

[tool.setuptools.packages.find]
where = ["."]
include = ["misc*", "charts*", "onstream*", "functional*"]
